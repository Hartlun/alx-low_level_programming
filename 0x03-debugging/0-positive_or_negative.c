#include "main.h"

/**
 * positive_or_negative - checks for positive or negative numers
 *@i: the number to be checked
 * Return: Always 0
*/

void positive_or_negative(int i)
{
	if (i < 0)
		printf("%d is positive", i);
	else if (i < 0)
		printf("%d is negative", i);
	return (0);
}
